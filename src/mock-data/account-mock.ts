import { AccountAmountCurrency } from '../app/modules/core/models/account-amount-currency';

export const accountMock: AccountAmountCurrency = Object.freeze({
  amount: 100.0,
  currencyCode: 'EUR',
});
