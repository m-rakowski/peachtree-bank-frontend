import { ColorRedOrGreenDirective } from './color-red-or-green.directive';

describe('ColorRedOrGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorRedOrGreenDirective();
    expect(directive).toBeTruthy();
  });
});
